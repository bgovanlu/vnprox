# T-906 · Map export (SVG/PNG) + print stylesheet — report

**Status:** complete. Committed by the orchestrator after the implementing agent finished the work but stalled at the commit step (it deferred to a background `make check` that had already completed); the orchestrator re-ran `make check` in the worktree to confirm green before committing.

## What was built
- `web/src/topology/export.ts` + `export.test.ts` — browser-independent SVG serialization of the current topology view's visible entity set (respecting layer toggles, VLAN filter, and collapse/LOD state), unit-tested in isolation from the download mechanism.
- `web/src/topology/exportDownload.ts` — the browser download / PNG-rasterization side effects, kept separate from the pure serialization so the logic is testable.
- `web/src/topology/ExportMapMenu.tsx` — the "Export map" toolbar control (Graph and Switch views), offering SVG and PNG.
- `web/src/index.css` — `@media print` stylesheet for the topology page: legible layout, interactive chrome (toolbars/drawers) hidden, current filter/legend state retained.
- `web/src/topology/TopologyCanvasV2.tsx` / `TopologyPage.tsx` — toolbar wiring for the export control.
- `web/e2e/map-export.spec.ts` — e2e: applies a layer toggle + VLAN filter, triggers SVG export, asserts the export contains only the filtered entity set.

## Verification
`make check` green in the worktree (golangci-lint 0, `go test ./...` pass, vitest 958/958, govulncheck + npm audit clean). Client-side only — **zero backend changes** (`internal/` and `docs/api.md` routes untouched); the existing `GET /export/doc` prose export is left as-is per the card.

## Notes for the integrator
- Modifies `TopologyCanvasV2.tsx` and `TopologyPage.tsx` — overlaps the topology files T-905/T-907 also touched; integration conflicts resolved at merge time by the orchestrator.
- Test count (958) reflects this worktree's base predating the T-903/T-904/T-905/T-907 merges; the integrated main tree is re-verified post-merge.
