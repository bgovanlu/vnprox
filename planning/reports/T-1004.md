# T-1004 · Host-local flow sampling (conntrack/eBPF) — report

**Status:** complete. Committed by the orchestrator after the implementing agent finished the work but stalled at the commit step (deferred to a background `make check`). The orchestrator re-ran `make check` in the worktree to confirm green before committing.

## What was built
- `internal/flow/hostsample/conntrack.go` — periodic `/proc/net/nf_conntrack` poll per bridge, diffed into `flow.Record`s tagged `source: "conntrack"`, interval configurable via `[flows] host_sample_interval_sec` (default 10). Feeds the **same** `internal/flow` ring store T-1002 built — no second storage path.
- `internal/flow/hostsample/ebpf.go` — eBPF per-bridge sampler behind a build tag + a runtime kernel-feature probe that refuses to start, logs why, and falls back to conntrack-only when the probe fails. CAP_BPF/CAP_PERFMON + kernel requirements documented in the package doc comment.
- Both samplers **off by default**, opt-in per node via `vnprox.toml`'s `[flows]` section; `GET /config`'s Settings payload surfaces the active sampler.
- `packaging/systemd/vnprox.service` + `packaging/debian/{postinst,postrm}` — the extra capability is granted **only** when `[flows] ebpf_sampling_enabled = true` at install/upgrade time, never unconditionally; `docs/security.md`'s Host footprint section updated to note this.
- `Makefile` — the eBPF build tag is excluded from the default `go test ./...` matrix (documented in the Makefile change).
- `cmd/vnproxd/hostsample.go` + tests — composition-root wiring of the samplers into the run group (opt-in gated).

## Verification
`make check` green in the worktree (golangci-lint 0, full `go test ./...`, vitest 1049/1049, govulncheck + npm audit clean). Conntrack sampler proven via a synthetic `/proc/net/nf_conntrack` fixture (incl. malformed-line-skip); the eBPF path exercised via the probe's negative case (clean conntrack-only fallback asserted by log output) — no CI eBPF attachment assumed.

## needs-hardware-validation
Appended 63 lines to `planning/reports/needs-hardware-validation.md` covering: conntrack table format across the PVE 8.2+/9.x kernel range, real CAP_BPF/CAP_PERFMON availability under the hardened systemd unit, measured CPU/memory overhead per sampler at the Phase 9 scale target with a concrete measurement plan, and eBPF verifier acceptance across the supported kernel range.

## Notes for the integrator
Touches `internal/config/config.go`, `internal/api/config.go`, `cmd/vnproxd/server.go`, docs, and packaging — the `[flows]` config section is shared with T-1002 (already merged) and extended additively here (adds `host_sample_interval_sec`, `conntrack_sampling_enabled`, `ebpf_sampling_enabled`). Integration conflicts (if any) resolved at merge time by the orchestrator.
