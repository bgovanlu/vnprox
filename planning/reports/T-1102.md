# T-1102 · Pinned-spec drift mode — report

**Status:** complete. Implemented by the subagent; committed by the orchestrator after the agent stalled at the commit step (deferred to a background `make check`). The orchestrator re-ran `make check` in the worktree to confirm green before committing.

## What was built
- `internal/store/migrations/0012_pinned_spec.sql` + `internal/store/pinnedspec.go` — the app-owned `pinned_spec` table (content, pinned_by, pinned_at).
- `internal/api/specpin.go` — `GET /spec/pin` (current pin + metadata), `POST /spec/pin` (netWrite+CSRF, audited `spec.pin`), `DELETE /spec/pin` (audited `spec.unpin`).
- `internal/drift/specdrift.go` — a `spec_drift` check family (source `drift`, `check: "spec_drift"` — a distinct sixth check value alongside the five cross-node families), computed each drift cycle by running T-1101's `spec.Import` against the pinned spec. `fixable: true`; the fix returns a draft changeset reconciling live→pin, never auto-applied.
- Scope inherited from T-1101's specVersion 1: reconcile (and therefore `spec_drift`) covers bond/bridge/vlan/sdn-zone/vnet/subnet (incl. DHCP ranges). **Firewall drift is not covered** — it is out of specVersion 1 per T-1101's documented scope.

## Verification
`make check` green in the worktree (golangci-lint 0, full `go test ./...`, vitest 1120/1120, govulncheck + npm audit clean). `spec_drift` clears on unpin; pin/unpin audited; the five existing cross-node families continue to fire independently (regression covered).

## Notes for the integrator
Touches `internal/api/router.go`, `cmd/vnproxd/{drift,server}.go`, `internal/drift/{service,types,doc}.go`, `internal/store/kv_test.go` (schema-version bump to 12), and docs — all also touched by in-flight siblings (T-1104 router/server, T-1107). The schema-version literal in `kv_test.go` was set to 12; it will need reconciling to the final max once T-1103 (0010), T-1104 (0011) and this (0012) all land. Integration conflicts resolved at merge time by the orchestrator.
