# T-1205 · Guarded switch config push — completion report

**Status:** implemented; backend `make check` (go build + go test) green. Committed by the orchestrator after the executing sub-agent stalled on a session limit at the report/commit step. Strong/review-checkpoint, highest-risk card: safety seams reviewed by the orchestrator before merge (see Review below).

## What was built
Guarded push of config to physical switches, entirely behind the change engine, shipping dark by default.
- `internal/switchdrv/` — `SwitchDriver` interface + driver scaffolding (feature-flagged-dark).
- `internal/switchmock/` — mock switch for tests.
- `internal/store/switches.go`, `internal/store/migrations/0022_switches.sql` — `switches` registry (per-switch explicit `enabled` opt-in). Schema version bumped to **22** in `kv_test.go`.
- `internal/change/params_switch.go`, `validate_switch.go` (+ `_test.go`) — `switch.port.update` op; `switchValidate` enforces, in order: `push_disabled` (daemon `[switches]` flag), `not_enabled` (unregistered/disabled switch), `port_not_pve_facing` (LLDP neighbor not a PVE node NIC), and the no-override `protected_switch_port` interlock (a net strip of the management VLAN from a mgmt-path uplink is hard-blocked). Fail-closed: nil/errored scope source rejects every op.
- Apply path: `apply.go`/`apply_exec.go`/`apply_plan.go`/`apply_seams.go`/`apply_snapshot.go` + `mgmttouch.go` extended so switch ops flow through stage → validate → diff → apply → commit-confirm → auto-rollback like every other op.
- `internal/topology/switches.go`, `internal/inventory/ref.go`, `internal/config/config.go` ([switches] flag), `internal/api/changesets.go`.

## Review (orchestrator, strong/highest-risk card)
- **Never applied outside the change engine:** `OpSwitchPortUpdate` is in the apply-plan op registry; `switchValidate` runs in the validate pipeline (`validate.go:130`). ✔
- **Ships dark:** daemon `[switches] enabled` defaults false; zero-value input fails every op closed. ✔
- **Mgmt-path interlock is un-overridable:** `protected_switch_port` is emitted from `switchValidate`, not `safetyValidate`, so `AllowDangerousOps` cannot downgrade it — matches the T-703/T-1103 no-override precedent. ✔
- **Scope is strict:** only PVE-facing uplink ports (LLDP-confirmed) are writable. ✔

## Testing
- `go build ./...` clean; `go test ./...` green in the task worktree, including `validate_switch_test.go` and apply-engine rollback/ordering tests.

## Migration note
Assigned migration **0022** by the orchestrator (repo advanced past the card's reserved number). Schema version literal → 22.

## Needs hardware validation
**Required.** Real switch driver behavior (vendor CLI/API, transactional apply, real rollback on a live switch) is unvalidated — developed against `internal/switchmock` only. The driver ships dark; do not enable `[switches]` in production until validated on target hardware.
