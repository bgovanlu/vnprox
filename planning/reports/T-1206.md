# T-1206 · PBS network awareness — completion report

**Status:** implemented; backend `make check` (go build + go test) green. Committed by the orchestrator after the executing sub-agent stalled on a session limit at the report/commit step (implementation was complete and tests passed).

## What was built
Read-only Proxmox Backup Server network awareness surfaced in topology (Proxmox remains source of truth; no shadow copy persisted).
- `internal/pbs/` — PBS discovery/awareness engine.
- `internal/pve/storage.go`, `internal/pvemock/storage.go`, `testdata/pbs/` — read-only PVE storage/PBS config plumbing + fixtures.
- `internal/api/pbs.go` (+ `_test.go`), `cmd/vnproxd/pbswire.go` — `GET /pbs` awareness route wired into the daemon.
- Integrations: `internal/topology/mgmtpath.go`, `internal/api/topology.go`, `internal/inventory/ref.go`, `internal/pvemock/{server,types}.go`, `internal/api/router.go`, `cmd/vnproxd/server.go`.

## Testing
- `go build ./...` clean; `go test ./...` green in the task worktree.
- No migration added (read-only awareness — nothing app-owned to persist).

## Deviations
None from the card's intent.

## Needs hardware validation
Real PBS datastore/network path reporting against a live PBS instance — developed against `internal/pvemock` fixtures only.
