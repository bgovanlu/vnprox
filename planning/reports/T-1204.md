# T-1204 · DNS management — completion report

**Status:** implemented; backend `make check` (go build + go test) green. Committed by the orchestrator after the executing sub-agent stalled on a session limit at the report/commit step (implementation was complete and tests passed).

## What was built
PVE SDN DNS zone/record management, all mutations flowing through the change engine (stage → validate → diff → apply → confirm/rollback). Proxmox is the source of truth.
- `internal/sdn/dns.go` (+ `_test.go`) — DNS model + logic.
- `internal/pve/sdn_dns.go`, `internal/pvemock/sdn_dns.go` — read/apply plumbing + mock fixtures.
- `internal/change/params_sdn_dns.go`, `apply_sdn.go` — new DNS op params + apply path; op registry/validation via `op.go`, `validate_*.go`, `validate_codes.go`.
- `internal/inventory/dns.go` + inventory `ingest/merge/ref` integration; `internal/topology/{project,types}.go`.
- `internal/api/sdn.go`, `internal/api/router.go`, `cmd/vnproxd/{changeagent,server}.go` — routes wired.

## Testing
- `go build ./...` clean; `go test ./...` green in the task worktree, including new `internal/sdn/dns_test.go` and change-engine validation/apply tests.
- No app-store migration added (DNS is PVE-owned SDN config).

## Deviations
None from the card's intent.

## Needs hardware validation
DNS apply against a live PVE SDN (plugin-specific behavior, e.g. PowerDNS) — developed against `internal/pvemock` only.
