# T-1007 · History playback — report

**Status:** complete. Implemented by the subagent; finalized (gofmt + lint fixes, report, commit) by the orchestrator after the agent stalled at the commit step (deferred to a background monitor). The orchestrator fixed the outstanding lint issues and verified before committing.

## What was built
- `web/src/topology/history/HistoryTimeline.tsx` — a scrubber bound to the map's existing traffic-paint (`trafficMode.ts`) and flows (`flowEdges.ts`) layers; dragging re-queries `GET /metrics/history` and `GET /flows?fromTs=&toTs=` for the selected instant/window and re-renders the *same* paint/edge logic with only a time parameter threaded through — no new rendering path.
- Event markers: changeset apply/confirm/rollback lifecycle rows (`GET /audit`) + finding transitions.
- `internal/findings/findingevents.go` — a new `Notifier` implementation recording each finding transition Engine already detects into the `finding_events` table (migration `internal/store/migrations/0009_finding_events.sql`, `{id, findingId, at, transition}`), composed alongside PVENotifier/WebhookNotifier via the existing `multiNotifier` at the composition root — it does NOT touch Engine's own once-per-transition firing logic, and reuses that transition detection rather than re-deriving one. Bounded to the same window as `metric_samples`, pruned alongside it.
- `internal/api/history.go` — `GET /history/events?fromTs=&toTs=` merging changeset-lifecycle audit rows and finding transitions into one marker feed.

## Read-only
The timeline exposes NO apply/confirm/rollback affordance; a changeset marker deep-links to the existing changeset detail/diff view, never re-triggers it. Playback range clamps to the shorter of the metrics (24h) and flows (default 60m) windows, and the UI discloses the flow-history limit explicitly.

## Verification
`make check`: gofmt clean, `go test ./...` green, vitest green, govulncheck + npm audit clean. golangci-lint: the real new code is clean; two residual `fieldalignment` reports referenced a sibling worktree path (`agent-a265464c1b0c05ba4`, the already-removed T-1003 worktree) — a golangci-lint cross-worktree cache artifact, confirmed absent when run from `main`.

Orchestrator fixes on top of the agent's work: gofmt `cmd/vnproxd/server.go`; auto-fixed three `fieldalignment` orderings in `internal/api/history_test.go` and `internal/findings/findingevents_test.go`; detached `findingevents.go`'s file comment from the `package findings` line (revive package-comments).

## Migration
`internal/store/migrations/0009_finding_events.sql` (0006/0007/0008 were taken by T-907/T-1002/T-1005).
