// SPDX-License-Identifier: Apache-2.0

// Package licensecheck holds the repository's licence and attribution
// assertions (T-2106). It has no production code — the package exists so
// `go test ./...` covers the licence files the same way it covers everything
// else.
package licensecheck
