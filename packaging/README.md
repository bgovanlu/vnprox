# packaging/

Placeholder directory. The .deb build, systemd unit, installer, and `vnproxctl` skeleton
are built here in **T-006** (`planning/tasks/phase-0.md`). Until then, `make deb` skips
with a notice instead of failing.
