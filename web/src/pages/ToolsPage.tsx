import { PlaceholderPage } from "./PlaceholderPage";

export function ToolsPage() {
  return (
    <PlaceholderPage
      title="Tools"
      description="The path simulator ('why can't VM A reach VM B?') and the raw interfaces editor land in a later task."
    />
  );
}
