import { PlaceholderPage } from "./PlaceholderPage";

export function IpamPage() {
  return (
    <PlaceholderPage
      title="IPAM"
      description="Subnet grids, allocation views, and conflict detection land in a later task."
    />
  );
}
