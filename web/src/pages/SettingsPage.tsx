import { PlaceholderPage } from "./PlaceholderPage";

export function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      description="Cluster, blueprint, and user preference settings land in a later task."
    />
  );
}
