import { PlaceholderPage } from "./PlaceholderPage";

export function AuditPage() {
  return (
    <PlaceholderPage
      title="Audit"
      description="A record of everything anyone changed lands in a later task."
    />
  );
}
