# web/

Placeholder directory. The React 18 + TypeScript (strict) + Vite SPA is built here in
**T-005** (`planning/tasks/phase-0.md`). Until then, `make build`/`make dev`/`make lint`
skip the frontend steps with a notice instead of failing.
